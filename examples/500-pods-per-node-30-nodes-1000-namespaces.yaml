# Example for creating 500 pods/node on a 30 node cluster, using pause
# pods
options:
  precleanup: true
  cleanup: true
  workload: pausepod
  deployments: 15
  namespaces: 1000
  exit-at-end: true
