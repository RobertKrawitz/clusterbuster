# Emacs autosave and backup files
*~
#*#
**/__pycache__/
.pytest_cache/
*.egg-info/
*.py[cod]
*$py.class
*.qcow2
examples/.cb-ordered-smoke.*.sh
.cursor/

# E2E test artifacts (report JSON, logs)
tests/e2e-artifacts/

# Local ClusterBuster CI report trees / tarballs for testing (not committed)
cb-ci-*/
cb-ci-*.tar
# Optional copy of a profile with local tweaks (e.g. smaller VM counts); not committed
lib/CI/profiles/*.local.yaml
