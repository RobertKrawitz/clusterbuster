# scripts

The legacy bash implementation of **`run-perf-ci-suite`** (`run-perf-ci-suite.sh`) lived here and has been **removed**. The supported entry point is the repo-root **[`run-perf-ci-suite`](../run-perf-ci-suite)** launcher (Python). See [`docs/clusterbuster-ci-python-phase2.md`](../docs/clusterbuster-ci-python-phase2.md).
